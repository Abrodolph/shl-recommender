# --- Runtime (pinned) ---------------------------------------------------------
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
python-dotenv==1.2.2

# Retrieval (dense query encoding + lexical). torch is pulled by
# sentence-transformers; the Dockerfile installs the CPU-only wheel to keep the
# image small.
sentence-transformers==5.6.0
rank-bm25==0.2.2
numpy==2.5.0

# LLM provider (behind app/llm interface; Gemini is an optional swap)
groq==1.5.0
# google-generativeai   # uncomment to use LLM_PROVIDER=gemini

# --- Tests --------------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1            # also required by fastapi.testclient

# --- Build-time only (NOT needed at runtime; catalog.json is already shipped) --
# Install manually to (re)scrape: pip install beautifulsoup4==4.12.3
